---
title: ASP.NET Core 2.1 から 2.2 への移行します。
author: scottaddie
description: この記事では、前提条件と ASP.NET Core 2.2 に、ASP.NET Core 2.1 のプロジェクトを移行する最も一般的な手順について説明します。
ms.author: scaddie
ms.custom: mvc
ms.date: 05/28/2019
uid: migration/21-to-22
ms.openlocfilehash: 7ebd3ca7108f406e89b8e6abc03d2b2df963a507
ms.sourcegitcommit: d6e51c60439f03a8992bda70cc982ddb15d3f100
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 07/03/2019
ms.locfileid: "67555916"
---
# <a name="migrate-from-aspnet-core-21-to-22"></a><span data-ttu-id="febe4-103">ASP.NET Core 2.1 から 2.2 への移行します。</span><span class="sxs-lookup"><span data-stu-id="febe4-103">Migrate from ASP.NET Core 2.1 to 2.2</span></span>

<span data-ttu-id="febe4-104">作成者: [Scott Addie](https://github.com/scottaddie)</span><span class="sxs-lookup"><span data-stu-id="febe4-104">By [Scott Addie](https://github.com/scottaddie)</span></span>

<span data-ttu-id="febe4-105">この記事では、ASP.NET Core 2.2 に既存の ASP.NET Core 2.1 プロジェクトを更新する方法について説明します。</span><span class="sxs-lookup"><span data-stu-id="febe4-105">This article explains how to update an existing ASP.NET Core 2.1 project to ASP.NET Core 2.2.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="febe4-106">必須コンポーネント</span><span class="sxs-lookup"><span data-stu-id="febe4-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="febe4-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="febe4-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs2019-2.2.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="febe4-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="febe4-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-2.2.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="febe4-109">Visual Studio for Mac</span><span class="sxs-lookup"><span data-stu-id="febe4-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-2.2.md)]

---

## <a name="update-target-framework-moniker-tfm"></a><span data-ttu-id="febe4-110">ターゲット フレームワーク モニカー (TFM) の更新</span><span class="sxs-lookup"><span data-stu-id="febe4-110">Update Target Framework Moniker (TFM)</span></span>

<span data-ttu-id="febe4-111">.NET Core をターゲットとするプロジェクトを使用する必要があります、 [TFM](/dotnet/standard/frameworks#referring-to-frameworks)の .NET Core 2.2 以上のバージョン。</span><span class="sxs-lookup"><span data-stu-id="febe4-111">Projects targeting .NET Core should use the [TFM](/dotnet/standard/frameworks#referring-to-frameworks) of a version greater than or equal to .NET Core 2.2.</span></span> <span data-ttu-id="febe4-112">プロジェクト ファイルでは、更新、`<TargetFramework>`でノードの内部テキ スト`netcoreapp2.2`:</span><span class="sxs-lookup"><span data-stu-id="febe4-112">In the project file, update the `<TargetFramework>` node's inner text with `netcoreapp2.2`:</span></span>

```xml
<TargetFramework>netcoreapp2.2</TargetFramework>
```

<span data-ttu-id="febe4-113">.NET Framework を対象とするプロジェクトが継続すると、.NET Framework 4.6.1 以上のバージョンの TFM を使用します。</span><span class="sxs-lookup"><span data-stu-id="febe4-113">Projects targeting .NET Framework may continue to use the TFM of a version greater than or equal to .NET Framework 4.6.1:</span></span>

```xml
<TargetFramework>net461</TargetFramework>
```

## <a name="adopt-the-iis-in-process-hosting-model"></a><span data-ttu-id="febe4-114">IIS プロセスでのホスティング モデルを採用します。</span><span class="sxs-lookup"><span data-stu-id="febe4-114">Adopt the IIS in-process hosting model</span></span>

<span data-ttu-id="febe4-115">採用する、 [IIS のプロセスでのホスティング モデル](xref:host-and-deploy/iis/index#in-process-hosting-model)、追加、`<AspNetCoreHostingModel>`プロパティの値を持つ`InProcess`を`<PropertyGroup>`プロジェクト ファイルで。</span><span class="sxs-lookup"><span data-stu-id="febe4-115">To adopt the [in-process hosting model for IIS](xref:host-and-deploy/iis/index#in-process-hosting-model), add the `<AspNetCoreHostingModel>` property with a value of `InProcess` to a `<PropertyGroup>` in the project file:</span></span>

```xml
<AspNetCoreHostingModel>InProcess</AspNetCoreHostingModel>
```

<span data-ttu-id="febe4-116">プロセスでのホスティング モデルは、.NET Framework を対象とする ASP.NET Core アプリのサポートされていません。</span><span class="sxs-lookup"><span data-stu-id="febe4-116">The in-process hosting model isn't supported for ASP.NET Core apps targeting .NET Framework.</span></span>

<span data-ttu-id="febe4-117">詳細については、「 <xref:host-and-deploy/aspnet-core-module#hosting-models> 」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="febe4-117">For more information, see <xref:host-and-deploy/aspnet-core-module#hosting-models>.</span></span>

## <a name="update-a-custom-webconfig-file"></a><span data-ttu-id="febe4-118">カスタムの web.config ファイルを更新します。</span><span class="sxs-lookup"><span data-stu-id="febe4-118">Update a custom web.config file</span></span>

<span data-ttu-id="febe4-119">カスタムを使用するプロジェクトの*web.config*のパブリッシュを生成するプロジェクトのルート ファイル*web.config*ファイル。</span><span class="sxs-lookup"><span data-stu-id="febe4-119">For projects that use a custom *web.config* file in the project root to generate their published *web.config* file:</span></span>

* <span data-ttu-id="febe4-120">`<handlers>` ASP.NET Core モジュールを追加するエントリ (`name="aspNetCore"`)、変更、`modules`属性から値`AspNetCoreModule`に`AspNetCoreModuleV2`します。</span><span class="sxs-lookup"><span data-stu-id="febe4-120">In the `<handlers>` entry that adds the ASP.NET Core Module (`name="aspNetCore"`), change the `modules` attribute value from `AspNetCoreModule` to `AspNetCoreModuleV2`.</span></span>
* <span data-ttu-id="febe4-121">`<aspNetCore>`要素、ホスティング モデル属性の追加 (`hostingModel="InProcess"`)。</span><span class="sxs-lookup"><span data-stu-id="febe4-121">In the `<aspNetCore>` element, add the hosting model attribute (`hostingModel="InProcess"`).</span></span>

<span data-ttu-id="febe4-122">詳細と例の*web.config*ファイルを参照してください<xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>します。</span><span class="sxs-lookup"><span data-stu-id="febe4-122">For more information and example *web.config* files, see <xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>.</span></span>

## <a name="update-package-references"></a><span data-ttu-id="febe4-123">パッケージ参照の更新</span><span class="sxs-lookup"><span data-stu-id="febe4-123">Update package references</span></span>

<span data-ttu-id="febe4-124">.NET Core を対象とする場合は、削除、メタパッケージ参照の`Version`プロジェクト ファイル内の属性。</span><span class="sxs-lookup"><span data-stu-id="febe4-124">If targeting .NET Core, remove the metapackage reference's `Version` attribute in the project file.</span></span> <span data-ttu-id="febe4-125">包含を`Version`で次の警告の属性します。</span><span class="sxs-lookup"><span data-stu-id="febe4-125">Inclusion of a `Version` attribute results in the following warning:</span></span>

```console
A PackageReference to 'Microsoft.AspNetCore.App' specified a Version of `2.2.0`. Specifying the version of this package is not recommended. For more information, see https://aka.ms/sdkimplicitrefs
```

<span data-ttu-id="febe4-126">詳細については、「 <xref:fundamentals/metapackage-app> 」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="febe4-126">For more information, see <xref:fundamentals/metapackage-app>.</span></span>

<span data-ttu-id="febe4-127">次のようになりますメタパッケージ リファレンス`<PackageReference />`ノード。</span><span class="sxs-lookup"><span data-stu-id="febe4-127">The metapackage reference should resemble the following `<PackageReference />` node:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore.App" />
</ItemGroup>
```

<span data-ttu-id="febe4-128">.NET Framework を対象とする場合は、各パッケージ参照を更新`Version`属性 2.2.0 以降。</span><span class="sxs-lookup"><span data-stu-id="febe4-128">If targeting .NET Framework, update each package reference's `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="febe4-129">.NET Framework を対象とする一般的な ASP.NET Core 2.2 プロジェクトでパッケージの参照を示します。</span><span class="sxs-lookup"><span data-stu-id="febe4-129">Here are the package references in a typical ASP.NET Core 2.2 project targeting .NET Framework:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.CookiePolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.HttpsPolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.Mvc" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.StaticFiles" Version="2.2.0" />
</ItemGroup>
```

<span data-ttu-id="febe4-130">参照する場合、 [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/)パッケージ、更新、`Version`属性 2.2.0 以降。</span><span class="sxs-lookup"><span data-stu-id="febe4-130">If referencing the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package, update its `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="febe4-131">エラーこれを行うには、次のエラーが得られます。</span><span class="sxs-lookup"><span data-stu-id="febe4-131">Failure to do so results in the following error:</span></span>

```console
Detected package downgrade: Microsoft.AspNetCore.Razor.Design from 2.2.0 to 2.1.2. Reference the package directly from the project to select a different version.
```

## <a name="update-net-core-sdk-version-in-globaljson"></a><span data-ttu-id="febe4-132">global.json での .NET Core SDK バージョンの更新</span><span class="sxs-lookup"><span data-stu-id="febe4-132">Update .NET Core SDK version in global.json</span></span>

<span data-ttu-id="febe4-133">ソリューションが依存する場合、 [global.json](/dotnet/core/tools/global-json)ファイルを特定の .NET Core SDK バージョンを対象に、更新、`version`プロパティをコンピューターにインストールされている、2.2 バージョン。</span><span class="sxs-lookup"><span data-stu-id="febe4-133">If your solution relies upon a [global.json](/dotnet/core/tools/global-json) file to target a specific .NET Core SDK version, update its `version` property to the 2.2 version installed on your machine:</span></span>

```json
{
  "sdk": {
    "version": "2.2.100"
  }
}
```

## <a name="update-launch-settings"></a><span data-ttu-id="febe4-134">更新プログラムの起動設定</span><span class="sxs-lookup"><span data-stu-id="febe4-134">Update launch settings</span></span>

<span data-ttu-id="febe4-135">プロジェクトの起動設定ファイルを更新して、Visual Studio Code を使用する場合 ( *.vscode/launch.json*)。</span><span class="sxs-lookup"><span data-stu-id="febe4-135">If using Visual Studio Code, update the project's launch settings file (*.vscode/launch.json*).</span></span> <span data-ttu-id="febe4-136">`program`パスは、新しい tfm 』 を参照する必要があります。</span><span class="sxs-lookup"><span data-stu-id="febe4-136">The `program` path should reference the new TFM:</span></span>

[!code-json[](21-to-22/samples/launch.json?highlight=9)]

## <a name="update-kestrel-configuration"></a><span data-ttu-id="febe4-137">Kestrel の構成を更新します。</span><span class="sxs-lookup"><span data-stu-id="febe4-137">Update Kestrel configuration</span></span>

<span data-ttu-id="febe4-138">アプリを呼び出す場合<xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*>呼び出して`CreateDefaultBuilder`で、 [CreateWebHostBuilder メソッド](xref:fundamentals/host/web-host#set-up-a-host)の`Program`クラスを呼び出す`ConfigureKestrel`の代わりに、Kestrel サーバーを構成する`UseKestrel`を回避するには競合しています、 [IIS インプロセス ホスティング モデル](xref:host-and-deploy/iis/index#in-process-hosting-model):</span><span class="sxs-lookup"><span data-stu-id="febe4-138">If the app calls <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> by calling `CreateDefaultBuilder` in the [CreateWebHostBuilder method](xref:fundamentals/host/web-host#set-up-a-host) of the `Program` class, call `ConfigureKestrel` to configure Kestrel server instead of `UseKestrel` in order to avoid conflicts with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model):</span></span>

```csharp
public static IWebHostBuilder CreateWebHostBuilder(string[] args) =>
    WebHost.CreateDefaultBuilder(args)
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        });
```

<span data-ttu-id="febe4-139">アプリが呼び出さない場合、`CreateDefaultBuilder`とホストを手動でビルド、`Program`クラスを呼び出す<xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*>**する前に**呼び出す`ConfigureKestrel`:</span><span class="sxs-lookup"><span data-stu-id="febe4-139">If the app doesn't call `CreateDefaultBuilder` and builds the host manually in the `Program` class, call <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> **before** calling `ConfigureKestrel`:</span></span>

```csharp
public static void Main(string[] args)
{
    var host = new WebHostBuilder()
        .UseContentRoot(Directory.GetCurrentDirectory())
        .UseKestrel()
        .UseIISIntegration()
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        })
        .Build();

    host.Run();
}
```

<span data-ttu-id="febe4-140">詳細については、「 <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps> 」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="febe4-140">For more information, see <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>.</span></span>

## <a name="update-compatibility-version"></a><span data-ttu-id="febe4-141">更新プログラムの互換性バージョン</span><span class="sxs-lookup"><span data-stu-id="febe4-141">Update compatibility version</span></span>

<span data-ttu-id="febe4-142">互換性バージョンを更新`Startup.ConfigureServices`に`Version_2_2`:</span><span class="sxs-lookup"><span data-stu-id="febe4-142">Update the compatibility version in `Startup.ConfigureServices` to `Version_2_2`:</span></span>

```csharp
services.AddMvc()
        .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

## <a name="update-cors-policy"></a><span data-ttu-id="febe4-143">CORS ポリシーを更新します。</span><span class="sxs-lookup"><span data-stu-id="febe4-143">Update CORS policy</span></span>

<span data-ttu-id="febe4-144">ASP.NET Core の 2.2 で、CORS ミドルウェアは、ワイルドカード オリジンで応答 (`*`) 場合は、ポリシーは、任意のオリジンを許可でき、資格情報。</span><span class="sxs-lookup"><span data-stu-id="febe4-144">In ASP.NET Core 2.2, the CORS middleware responds with a wildcard origin (`*`) if a policy allows any origin and allows credentials.</span></span> <span data-ttu-id="febe4-145">資格情報には、ワイルドカード オリジンときにサポートされていません (`*`) が指定されているブラウザーが CORS 要求を許可しないとします。</span><span class="sxs-lookup"><span data-stu-id="febe4-145">Credentials aren't supported when a wildcard origin (`*`) is specified, and browsers will disallow the CORS request.</span></span> <span data-ttu-id="febe4-146">詳細については、クライアントでは、問題を修正するためのオプションなどを参照してください、 [MDN web docs](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials)します。</span><span class="sxs-lookup"><span data-stu-id="febe4-146">For more information, including options for correcting the problem on the client, see the [MDN web docs](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials).</span></span>

<span data-ttu-id="febe4-147">サーバー上でこの問題を修正するには、次の操作のいずれかを実行します。</span><span class="sxs-lookup"><span data-stu-id="febe4-147">To correct this problem on the server, take one of the following actions:</span></span>

* <span data-ttu-id="febe4-148">不要になった資格情報を許可する CORS ポリシーを変更します。</span><span class="sxs-lookup"><span data-stu-id="febe4-148">Modify the CORS policy to no longer allow credentials.</span></span> <span data-ttu-id="febe4-149">つまりへの呼び出しを削除<xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*>ポリシーを構成するときにします。</span><span class="sxs-lookup"><span data-stu-id="febe4-149">That is, remove the call to <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> when configuring the policy.</span></span>
* <span data-ttu-id="febe4-150">資格情報が CORS 要求を成功させるために必要な場合は、許可されているホストを指定するポリシーを変更します。</span><span class="sxs-lookup"><span data-stu-id="febe4-150">If credentials are required for the CORS request to succeed, modify the policy to specify allowed hosts.</span></span> <span data-ttu-id="febe4-151">たとえば、使用して`builder.WithOrigins("https://api.example1.com", "https://example2.com")`を使用してではなく<xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>します。</span><span class="sxs-lookup"><span data-stu-id="febe4-151">For example, use `builder.WithOrigins("https://api.example1.com", "https://example2.com")` instead of using <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>.</span></span>

## <a name="update-docker-images"></a><span data-ttu-id="febe4-152">Docker イメージを更新します。</span><span class="sxs-lookup"><span data-stu-id="febe4-152">Update Docker images</span></span>

<span data-ttu-id="febe4-153">次の表は、Docker イメージ タグの変更内容を示しています。</span><span class="sxs-lookup"><span data-stu-id="febe4-153">The following table shows the Docker image tag changes:</span></span>

| <span data-ttu-id="febe4-154">2.1</span><span class="sxs-lookup"><span data-stu-id="febe4-154">2.1</span></span>                                       | <span data-ttu-id="febe4-155">2.2</span><span class="sxs-lookup"><span data-stu-id="febe4-155">2.2</span></span>                                       |
| ----------------------------------------- | ----------------------------------------- |
| `microsoft/dotnet:2.1-aspnetcore-runtime` | `microsoft/dotnet:2.2-aspnetcore-runtime` |
| `microsoft/dotnet:2.1-sdk`                | `microsoft/dotnet:2.2-sdk`                |

<span data-ttu-id="febe4-156">変更、`FROM`内の行、 *Dockerfile* 2.2 の列を前の表で、新しいイメージのタグを使用します。</span><span class="sxs-lookup"><span data-stu-id="febe4-156">Change the `FROM` lines in your *Dockerfile* to use the new image tags in the preceding table's 2.2 column.</span></span>

## <a name="build-manually-in-visual-studio-when-using-iis-in-process-hosting"></a><span data-ttu-id="febe4-157">IIS のインプロセス ホストを使用する場合、Visual Studio で手動で構築します。</span><span class="sxs-lookup"><span data-stu-id="febe4-157">Build manually in Visual Studio when using IIS in-process hosting</span></span>

<span data-ttu-id="febe4-158">Visual Studio の**ブラウザーの要求で自動ビルド**でエクスペリエンスが機能しません、 [IIS インプロセス ホスティング モデル](xref:host-and-deploy/iis/index#in-process-hosting-model)します。</span><span class="sxs-lookup"><span data-stu-id="febe4-158">Visual Studio's **Auto build on browser request** experience doesn't function with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model).</span></span> <span data-ttu-id="febe4-159">インプロセス ホストを使用する場合は、手動でプロジェクトをリビルドする必要があります。</span><span class="sxs-lookup"><span data-stu-id="febe4-159">You must manually rebuild the project when using in-process hosting.</span></span> <span data-ttu-id="febe4-160">Visual Studio の今後のリリースでは、このエクスペリエンスの改善が予定です。</span><span class="sxs-lookup"><span data-stu-id="febe4-160">Improvements to this experience are planned for a future release of Visual Studio.</span></span>

## <a name="update-logging-code"></a><span data-ttu-id="febe4-161">ログ記録のコードを更新します。</span><span class="sxs-lookup"><span data-stu-id="febe4-161">Update logging code</span></span>

<span data-ttu-id="febe4-162">2\.2 を 2.1 から推奨されているログの構成コードを変更していないが、1.x コーディング パターンをいくつかまだ 2.1 で不要になった作業が 2.2 で機能します。</span><span class="sxs-lookup"><span data-stu-id="febe4-162">Recommended logging configuration code didn't change from 2.1 to 2.2, but some 1.x coding patterns that still worked in 2.1 no longer work in 2.2.</span></span>

<span data-ttu-id="febe4-163">場合、アプリはログ プロバイダーの初期化、フィルター、および構成の読み込み、`Startup`クラス、そのコードを移動`Program.Main`:</span><span class="sxs-lookup"><span data-stu-id="febe4-163">If your app does logging provider initialization, filtering, and configuration loading in the `Startup` class, move that code to `Program.Main`:</span></span>

* <span data-ttu-id="febe4-164">プロバイダーの初期化:</span><span class="sxs-lookup"><span data-stu-id="febe4-164">Provider initialization:</span></span>

  <span data-ttu-id="febe4-165">1.x の例:</span><span class="sxs-lookup"><span data-stu-id="febe4-165">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole();
  }
  ```

  <span data-ttu-id="febe4-166">2.2 の例:</span><span class="sxs-lookup"><span data-stu-id="febe4-166">2.2 example:</span></span>

  ```csharp

  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole();
          })
          // ...
  }
  ```

* <span data-ttu-id="febe4-167">フィルター処理:</span><span class="sxs-lookup"><span data-stu-id="febe4-167">Filtering:</span></span>

  <span data-ttu-id="febe4-168">1.x の例:</span><span class="sxs-lookup"><span data-stu-id="febe4-168">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(LogLevel.Information);
      // or
      loggerFactory.AddConsole((category, level) => 
          category == "A" || level == LogLevel.Critical);
  }
  ```

  <span data-ttu-id="febe4-169">2.2 の例:</span><span class="sxs-lookup"><span data-stu-id="febe4-169">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole()
                     .AddFilter<ConsoleLoggerProvider>
                         (category: null, level: LogLevel.Information)
                     // or
                     .AddFilter<ConsoleLoggerProvider>
                         ((category, level) => category == "A" ||
                             level == LogLevel.Critical)
              );
          })
          // ...
  }
  ```

* <span data-ttu-id="febe4-170">構成の読み込み:</span><span class="sxs-lookup"><span data-stu-id="febe4-170">Configuration loading:</span></span>

  <span data-ttu-id="febe4-171">1.x の例:</span><span class="sxs-lookup"><span data-stu-id="febe4-171">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(Configuration);
  }
  ```

  <span data-ttu-id="febe4-172">2.2 の例:</span><span class="sxs-lookup"><span data-stu-id="febe4-172">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConfiguration(hostingContext.Configuration.GetSection("Logging"));
              logging.AddConsole();
          })
          // ...
  }
  ```

<span data-ttu-id="febe4-173">詳細については、「<xref:fundamentals/logging/index>」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="febe4-173">For more information, see <xref:fundamentals/logging/index></span></span>

## <a name="additional-resources"></a><span data-ttu-id="febe4-174">その他の技術情報</span><span class="sxs-lookup"><span data-stu-id="febe4-174">Additional resources</span></span>

* <xref:mvc/compatibility-version>
* <xref:fundamentals/metapackage-app>
* [<span data-ttu-id="febe4-175">暗黙的なパッケージ参照</span><span class="sxs-lookup"><span data-stu-id="febe4-175">Implicit package references</span></span>](/dotnet/core/tools/csproj#implicit-package-references)
