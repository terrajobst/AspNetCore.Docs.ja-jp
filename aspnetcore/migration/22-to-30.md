---
title: ASP.NET Core 2.2 から3.0 プレビューへの移行
author: tdykstra
description: ASP.NET Core 2.2 プロジェクトを ASP.NET Core 3.0 に移行する方法について説明します。
ms.author: tdykstra
ms.custom: mvc
ms.date: 08/05/2019
uid: migration/22-to-30
ms.openlocfilehash: 6312feb05cc917dd28e40b01f51a5fbf31c59e37
ms.sourcegitcommit: 2eb605f4f20ac4dd9de6c3b3e3453e108a357a21
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 08/06/2019
ms.locfileid: "68819887"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="550db-103">ASP.NET Core 2.2 から3.0 への移行</span><span class="sxs-lookup"><span data-stu-id="550db-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="550db-104">[Scott Addie](https://github.com/scottaddie)と[Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="550db-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="550db-105">この記事では、既存の ASP.NET Core 2.2 プロジェクトを ASP.NET Core 3.0 に更新する方法について説明します。</span><span class="sxs-lookup"><span data-stu-id="550db-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="550db-106">必須コンポーネント</span><span class="sxs-lookup"><span data-stu-id="550db-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="550db-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="550db-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="550db-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="550db-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="550db-109">Visual Studio for Mac</span><span class="sxs-lookup"><span data-stu-id="550db-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="550db-110">プロジェクトファイルを更新する</span><span class="sxs-lookup"><span data-stu-id="550db-110">Update the project file</span></span>

* <span data-ttu-id="550db-111">[ターゲットフレームワークモニカー (TFM)](/dotnet/standard/frameworks)を次の`netcoreapp3.0`ように設定します。</span><span class="sxs-lookup"><span data-stu-id="550db-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="550db-112">[AspNetCore また](xref:fundamentals/metapackage)は[AspNetCore](xref:fundamentals/metapackage-app)メタパッケージのすべてのを削除します。`<PackageReference>`</span><span class="sxs-lookup"><span data-stu-id="550db-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="550db-113">[AspNetCore パッケージ](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/)のを削除し`<PackageReference>`ます。</span><span class="sxs-lookup"><span data-stu-id="550db-113">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package.</span></span>

* <span data-ttu-id="550db-114">`<PackageReference>` `3.0.0-preview5-19227-01`パッケージの残り`Microsoft.AspNetCore.*`の要素の属性を現在のプレビューに更新します(`Version`たとえば、)。</span><span class="sxs-lookup"><span data-stu-id="550db-114">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="550db-115">パッケージの3.0 バージョンがない場合、パッケージは3.0 で非推奨とされている可能性があります。</span><span class="sxs-lookup"><span data-stu-id="550db-115">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="550db-116">これらのパッケージの多くは、 `Microsoft.AspNetCore.App`の一部であり、個別に参照することはできません。</span><span class="sxs-lookup"><span data-stu-id="550db-116">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="550db-117">3\.0 で生成されなくなったパッケージの暫定的な一覧については、「 [3.0 での共有フレームワークアセンブリのパッケージ生成の停止 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756)」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="550db-117">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="550db-118">*共有フレームワーク*は、コンピューターにインストールされ 、によっ`Microsoft.AspNetCore.App`て参照されるアセンブリ (.dll ファイル) のセットです。</span><span class="sxs-lookup"><span data-stu-id="550db-118">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="550db-119">詳しくは、[共有フレームワーク](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/)に関するページをご覧ください。</span><span class="sxs-lookup"><span data-stu-id="550db-119">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="550db-120">注目すべきいくつかのコンポーネントのアセンブリ`Microsoft.AspNetCore.App`は、3.0 から削除されました。</span><span class="sxs-lookup"><span data-stu-id="550db-120">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="550db-121">AspNetCore `<PackageReference>`から削除するアセンブリに一覧表示されているパッケージの api を使用している場合は、要素を追加します。 [3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755)。</span><span class="sxs-lookup"><span data-stu-id="550db-121">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="550db-122">削除されるコンポーネントの例を次に示します。</span><span class="sxs-lookup"><span data-stu-id="550db-122">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="550db-123">で`Microsoft.AspNetCore.App`出荷されたアセンブリの一覧は完了しておらず、3.0 RTM より前に変更されます。</span><span class="sxs-lookup"><span data-stu-id="550db-123">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="550db-124">次のコードがあるとします。</span><span class="sxs-lookup"><span data-stu-id="550db-124">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="550db-125">前`ReadAsAsync`のコードで呼び出されたメソッドは、 `Microsoft.AspNet.WebApi.Client`に含まれています。</span><span class="sxs-lookup"><span data-stu-id="550db-125">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="550db-126">3\.0 でコンパイルの問題を解決するには、 [WebApi](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet パッケージをインストールします。</span><span class="sxs-lookup"><span data-stu-id="550db-126">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="550db-127">[Json.NET サポート](#jsonnet-support)を追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-127">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="550db-128">プロジェクトは、既定では ASP.NET Core 3.0 以降のインプロセス[ホスティングモデル](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model)に設定されます。</span><span class="sxs-lookup"><span data-stu-id="550db-128">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="550db-129">必要に応じて、 `<AspNetCoreHostingModel>`プロジェクトファイルのプロパティの値が`InProcess`の場合は、そのプロパティを削除することもできます。</span><span class="sxs-lookup"><span data-stu-id="550db-129">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="550db-130">Json.NET のサポート</span><span class="sxs-lookup"><span data-stu-id="550db-130">Json.NET support</span></span>

<span data-ttu-id="550db-131">[ASP.NET Core 共有フレームワークを改善](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)する作業の一環として、ASP.NET Core 共有フレームワークから[Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm)が削除されています。</span><span class="sxs-lookup"><span data-stu-id="550db-131">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="550db-132">ASP.NET Core 3.0 プロジェクトで Json.NET を使用するには、次のようにします。</span><span class="sxs-lookup"><span data-stu-id="550db-132">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="550db-133">パッケージへの参照を[AspNetCore](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)に追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-133">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="550db-134">を`Startup.ConfigureServices`呼び出す`AddNewtonsoftJson`ように更新します。</span><span class="sxs-lookup"><span data-stu-id="550db-134">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="550db-135">`AddNewtonsoftJson`は、新しい MVC サービスの登録方法と互換性があります。</span><span class="sxs-lookup"><span data-stu-id="550db-135">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="550db-136">Json.NET の設定は、の呼び出し`AddNewtonsoftJson`で設定できます。</span><span class="sxs-lookup"><span data-stu-id="550db-136">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="550db-137">MVC サービスの登録</span><span class="sxs-lookup"><span data-stu-id="550db-137">MVC service registration</span></span>

<span data-ttu-id="550db-138">ASP.NET Core 3.0 では、内`Startup.ConfigureServices`に MVC シナリオを登録するための新しいオプションが追加されています。</span><span class="sxs-lookup"><span data-stu-id="550db-138">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="550db-139">の`IServiceCollection` MVC シナリオに関連する3つの新しい最上位レベルの拡張メソッドを使用できます。</span><span class="sxs-lookup"><span data-stu-id="550db-139">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="550db-140">テンプレートでは、ではなく`UseMvc`、これらの新しいメソッドを使用します。</span><span class="sxs-lookup"><span data-stu-id="550db-140">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="550db-141">ただし、 `AddMvc`は、以前のリリースと同じように動作します。</span><span class="sxs-lookup"><span data-stu-id="550db-141">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="550db-142">次の例では、コントローラーと API 関連の機能のサポートを追加しますが、ビューやページはサポートしません。</span><span class="sxs-lookup"><span data-stu-id="550db-142">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="550db-143">API テンプレートでは、次のコードを使用します。</span><span class="sxs-lookup"><span data-stu-id="550db-143">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="550db-144">次の例では、コントローラー、API 関連の機能、およびビューのサポートを追加しますが、ページはサポートしません。</span><span class="sxs-lookup"><span data-stu-id="550db-144">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="550db-145">Web アプリケーション (MVC) テンプレートでは、次のコードを使用します。</span><span class="sxs-lookup"><span data-stu-id="550db-145">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="550db-146">次の例では、Razor Pages と最小コントローラーサポートのサポートが追加されています。</span><span class="sxs-lookup"><span data-stu-id="550db-146">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="550db-147">Web アプリケーションテンプレートでは、次のコードを使用します。</span><span class="sxs-lookup"><span data-stu-id="550db-147">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="550db-148">新しいメソッドを組み合わせることもできます。</span><span class="sxs-lookup"><span data-stu-id="550db-148">The new methods can also be combined.</span></span> <span data-ttu-id="550db-149">次の例は ASP.NET Core 2.2 で`AddMvc`を呼び出すことと同じです。</span><span class="sxs-lookup"><span data-stu-id="550db-149">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="550db-150">ルーティングのスタートアップコードを更新する</span><span class="sxs-lookup"><span data-stu-id="550db-150">Update routing startup code</span></span>

<span data-ttu-id="550db-151">アプリがまたは`UseMvc` `UseSignalR`を呼び出す場合は、可能であれば、[エンドポイントルーティング](xref:fundamentals/routing)にアプリを移行します。</span><span class="sxs-lookup"><span data-stu-id="550db-151">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="550db-152">以前のバージョンの MVC とのエンドポイントルーティングの互換性を向上させるために、ASP.NET Core 2.2 で導入された URL 生成の一部の変更を元に戻しました。</span><span class="sxs-lookup"><span data-stu-id="550db-152">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="550db-153">2\.2 でエンドポイントルーティングを使用するときに問題が発生した場合は、次の例外が発生して ASP.NET Core 3.0 が改善されることを期待しています。</span><span class="sxs-lookup"><span data-stu-id="550db-153">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="550db-154">アプリがを実装`IRouter`している`Route`か、を継承している場合は、この時点で移行を回避することができます。</span><span class="sxs-lookup"><span data-stu-id="550db-154">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="550db-155">[IRouter ベースの実装をエンドポイントルーティングに移行する計画](https://github.com/aspnet/AspNetCore/issues/4221)に関するフィードバックを提供します。</span><span class="sxs-lookup"><span data-stu-id="550db-155">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="550db-156">アプリが MVC 内で`RouteData.Routers`直接アクセスする場合は、現時点で移行を回避することができます。</span><span class="sxs-lookup"><span data-stu-id="550db-156">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="550db-157">[RouteData. ルーターを使用するための移行ガイダンス](https://github.com/aspnet/AspNetCore/issues/9148)に関するフィードバックを提供します。</span><span class="sxs-lookup"><span data-stu-id="550db-157">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="550db-158">エンドポイントルーティングでは、と同じルートパターン構文とルートパターン`IRouter`作成機能がサポートされています。</span><span class="sxs-lookup"><span data-stu-id="550db-158">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="550db-159">エンドポイントルーティング`IRouteContraint`は、をサポートします。</span><span class="sxs-lookup"><span data-stu-id="550db-159">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="550db-160">エンドポイントルーティング`[Route]`で`[HttpGet]`は、、、およびその他の MVC ルーティング属性がサポートされます。</span><span class="sxs-lookup"><span data-stu-id="550db-160">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="550db-161">ほとんどのアプリケーションでは`Startup` 、変更が必要になるだけです。</span><span class="sxs-lookup"><span data-stu-id="550db-161">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="550db-162">スタートアップを移行します。構成</span><span class="sxs-lookup"><span data-stu-id="550db-162">Migrate Startup.Configure</span></span>

<span data-ttu-id="550db-163">一般的なアドバイス:</span><span class="sxs-lookup"><span data-stu-id="550db-163">General advice:</span></span>

* <span data-ttu-id="550db-164">を`UseRouting`追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-164">Add `UseRouting`.</span></span> 
* <span data-ttu-id="550db-165">アプリがを呼び`UseStaticFiles`出す場合`UseStaticFiles`は、の**前に** `UseRouting`配置します。</span><span class="sxs-lookup"><span data-stu-id="550db-165">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="550db-166">アプリ`AuthorizePage`がや `UseCors` `UseAuthorization` `UseAuthentication` `UseRouting`などの認証/承認機能を使用している場合は、(CORS ミドルウェアが使用されている場合は) の後にとの呼び出しを配置します。 `[Authorize]`</span><span class="sxs-lookup"><span data-stu-id="550db-166">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting` (and **after** `UseCors` if CORS Middleware is used).</span></span>
* <span data-ttu-id="550db-167">また`UseMvc`は`UseSignalR`をに`UseEndpoints`置き換えます。</span><span class="sxs-lookup"><span data-stu-id="550db-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>
* <span data-ttu-id="550db-168">アプリで[cors](xref:security/cors)シナリオ`[EnableCors]`(など) を使用する場合は、cors `UseCors`を使用する他のすべてのミドルウェアの前に、への`UseAuthorization`呼び出しを配置します (たとえば、、、および`UseMvc`の前`UseAuthentication`に配置`UseCors`します)。</span><span class="sxs-lookup"><span data-stu-id="550db-168">If the app uses [CORS](xref:security/cors) scenarios, such as `[EnableCors]`, place the call to `UseCors` before any other middlewares that use CORS (for example, place `UseCors` before `UseAuthentication`, `UseAuthorization`, and `UseMvc`).</span></span>

<span data-ttu-id="550db-169">一般的な ASP.NET Core 2.2 アプリの`Startup.Configure`の例を次に示します。</span><span class="sxs-lookup"><span data-stu-id="550db-169">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="550db-170">前`Startup.Configure`のコードを更新した後:</span><span class="sxs-lookup"><span data-stu-id="550db-170">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseRouting();

    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="550db-171">セキュリティミドルウェアのガイダンス</span><span class="sxs-lookup"><span data-stu-id="550db-171">Security middleware guidance</span></span>

<span data-ttu-id="550db-172">承認と CORS のサポートは、[ミドルウェア](xref:fundamentals/middleware/index)アプローチに関連して統合されています。</span><span class="sxs-lookup"><span data-stu-id="550db-172">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="550db-173">これにより、これらのシナリオで同じミドルウェアと機能を使用できるようになります。</span><span class="sxs-lookup"><span data-stu-id="550db-173">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="550db-174">このリリースでは、更新された承認ミドルウェアが提供されており、CORS ミドルウェアが拡張され、MVC コントローラーで使用される属性を理解できるようになりました。</span><span class="sxs-lookup"><span data-stu-id="550db-174">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="550db-175">CORS</span><span class="sxs-lookup"><span data-stu-id="550db-175">CORS</span></span>

<span data-ttu-id="550db-176">以前は、CORS を構成するのは簡単ではありませんでした。</span><span class="sxs-lookup"><span data-stu-id="550db-176">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="550db-177">ミドルウェアは一部のユースケースで使用するために提供されていましたが、MVC フィルターは他のユースケースでミドルウェア**なし**で使用することを意図していました。</span><span class="sxs-lookup"><span data-stu-id="550db-177">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="550db-178">ASP.NET Core 3.0 では、CORS を必要とするすべてのアプリで、エンドポイントルーティングと共に CORS ミドルウェアを使用することをお勧めします。</span><span class="sxs-lookup"><span data-stu-id="550db-178">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="550db-179">`UseCors`既定のポリシー `[EnableCors]`を使用して指定できます`[DisableCors]` 。また、属性を使用して、必要に応じて既定のポリシーを上書きすることもできます。</span><span class="sxs-lookup"><span data-stu-id="550db-179">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="550db-180">次に例を示します。</span><span class="sxs-lookup"><span data-stu-id="550db-180">In the following example:</span></span>

* <span data-ttu-id="550db-181">CORS は、 `default`名前付きポリシーが設定されたすべてのエンドポイントに対して有効になります。</span><span class="sxs-lookup"><span data-stu-id="550db-181">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="550db-182">クラス`MyController`は、 `[DisableCors]`属性を使用して CORS を無効にします。</span><span class="sxs-lookup"><span data-stu-id="550db-182">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="550db-183">承認</span><span class="sxs-lookup"><span data-stu-id="550db-183">Authorization</span></span>

<span data-ttu-id="550db-184">以前のバージョンの ASP.NET Core では、 `[Authorize]`属性を介して承認のサポートが提供されていました。</span><span class="sxs-lookup"><span data-stu-id="550db-184">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="550db-185">承認ミドルウェアを使用できませんでした。</span><span class="sxs-lookup"><span data-stu-id="550db-185">Authorization middleware wasn't available.</span></span> <span data-ttu-id="550db-186">ASP.NET Core 3.0 では、承認ミドルウェアが必要です。</span><span class="sxs-lookup"><span data-stu-id="550db-186">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="550db-187">ASP.NET Core 承認ミドルウェア (`UseAuthorization`) は、の`UseAuthentication`直後に配置することをお勧めします。</span><span class="sxs-lookup"><span data-stu-id="550db-187">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="550db-188">承認ミドルウェアは、既定のポリシーを使用して構成することもできます。これは上書きできます。</span><span class="sxs-lookup"><span data-stu-id="550db-188">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="550db-189">ASP.NET Core 3.0 `UseAuthorization`以降では、はで`Startup.Configure`呼び出され、次`HomeController`のユーザーにはサインインユーザーが必要です。</span><span class="sxs-lookup"><span data-stu-id="550db-189">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="550db-190">アプリが MVC でグローバル`AuthorizeFilter`フィルターとしてを使用している場合は、 `UseAuthorization`ミドルウェアにポリシーを提供するようにコードをリファクタリングすることをお勧めします。</span><span class="sxs-lookup"><span data-stu-id="550db-190">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="550db-191">次の例では、が呼び出されたとき`UseAuthorization`にすべての`HomeController`要求に適用されるカスタムポリシーと、ユーザーがアプリにサインインしなくてもアクセスできるようにします。</span><span class="sxs-lookup"><span data-stu-id="550db-191">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="550db-192">ミドルウェアによる承認は、フレームワークが承認に関する特定の知識を持っていなくても機能します。</span><span class="sxs-lookup"><span data-stu-id="550db-192">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="550db-193">たとえば、[正常性チェック](xref:host-and-deploy/health-checks)には承認に関する特定の情報はありませんが、正常性チェックには、ミドルウェアによって適用可能な承認ポリシーが設定されている可能性があります。</span><span class="sxs-lookup"><span data-stu-id="550db-193">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="550db-194">次の例では`UseAuthorization` 、は既定のポリシーを使用せず`/healthz`に承認を処理しますが、正常性`admin`チェックのエンドポイントではユーザーがロールに含まれている必要があります。</span><span class="sxs-lookup"><span data-stu-id="550db-194">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="550db-195">一部のシナリオでは、保護が実装されています。</span><span class="sxs-lookup"><span data-stu-id="550db-195">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="550db-196">`UseEndpoint`ミドルウェアが不足しているために承認ポリシーまたは CORS ポリシーがスキップされた場合、ミドルウェアは例外をスローします。</span><span class="sxs-lookup"><span data-stu-id="550db-196">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="550db-197">構成の誤りに関する追加のフィードバックを提供するアナライザーのサポートが進行中です。</span><span class="sxs-lookup"><span data-stu-id="550db-197">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="550db-198">SignalR の移行</span><span class="sxs-lookup"><span data-stu-id="550db-198">Migrate SignalR</span></span>

<span data-ttu-id="550db-199">SignalR hub のマッピングは、の内部`UseEndpoints`で行われるようになりました。</span><span class="sxs-lookup"><span data-stu-id="550db-199">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="550db-200">各ハブをに`MapHub`マップします。</span><span class="sxs-lookup"><span data-stu-id="550db-200">Map each hub with `MapHub`.</span></span> <span data-ttu-id="550db-201">以前のバージョンと同様に、各ハブが明示的に一覧表示されています。</span><span class="sxs-lookup"><span data-stu-id="550db-201">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="550db-202">次の例では、 `ChatHub` SignalR hub のサポートが追加されています。</span><span class="sxs-lookup"><span data-stu-id="550db-202">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="550db-203">MVC コントローラーの移行</span><span class="sxs-lookup"><span data-stu-id="550db-203">Migrate MVC controllers</span></span>

<span data-ttu-id="550db-204">コントローラーのマッピングは、の内部`UseEndpoints`で行われるようになりました。</span><span class="sxs-lookup"><span data-stu-id="550db-204">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="550db-205">アプリ`MapControllers`が属性ルーティングを使用する場合は、を追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-205">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="550db-206">ルーティングには ASP.NET Core 3.0 以降の多くのフレームワークのサポートが含まれているため、属性ルーティングコントローラーの追加はオプトインされています。</span><span class="sxs-lookup"><span data-stu-id="550db-206">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="550db-207">次の部分を探します。</span><span class="sxs-lookup"><span data-stu-id="550db-207">Replace the following:</span></span>

* <span data-ttu-id="550db-208">`MapRoute`で`MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="550db-208">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="550db-209">`MapAreaRoute`で`MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="550db-209">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="550db-210">現在、ルーティングには MVC 以外のサポートが含まれているので、これらの方法を明確に示すために用語が変更されています。</span><span class="sxs-lookup"><span data-stu-id="550db-210">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="550db-211">など`MapControllerRoute`の従来の`MapDefaultControllerRoute`ルート/ `MapAreaControllerRoute` は、追加された順序で適用されます。/</span><span class="sxs-lookup"><span data-stu-id="550db-211">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="550db-212">まず、特定のルート (区分のルートなど) を配置します。</span><span class="sxs-lookup"><span data-stu-id="550db-212">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="550db-213">次に例を示します。</span><span class="sxs-lookup"><span data-stu-id="550db-213">In the following example:</span></span>

* <span data-ttu-id="550db-214">`MapControllers`属性ルーティングコントローラーのサポートを追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-214">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="550db-215">`MapAreaControllerRoute`領域内にコントローラーの従来のルートを追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-215">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="550db-216">`MapControllerRoute`コントローラーの従来のルートを追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-216">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="550db-217">移行 Razor Pages</span><span class="sxs-lookup"><span data-stu-id="550db-217">Migrate Razor Pages</span></span>

<span data-ttu-id="550db-218">マッピング Razor Pages は、の内部`UseEndpoints`で行われるようになりました。</span><span class="sxs-lookup"><span data-stu-id="550db-218">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="550db-219">アプリ`MapRazorPages`が Razor Pages を使用する場合は、を追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-219">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="550db-220">エンドポイントルーティングには多くのフレームワークがサポートされているため、Razor Pages の追加はオプトインになりました。</span><span class="sxs-lookup"><span data-stu-id="550db-220">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="550db-221">次の例では`MapRazorPages` 、に Razor Pages のサポートが追加されています。</span><span class="sxs-lookup"><span data-stu-id="550db-221">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="550db-222">エンドポイントルーティングなしで MVC を使用する</span><span class="sxs-lookup"><span data-stu-id="550db-222">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="550db-223">`UseMvc`または ASP.NET Core 3.0 `UseMvcWithDefaultRoute`で MVC を使用するには、内`Startup.ConfigureServices`に明示的なオプトインが必要です。</span><span class="sxs-lookup"><span data-stu-id="550db-223">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="550db-224">これは、MVC が初期化中に承認と CORS ミドルウェアに依存できるかどうかを確認する必要があるためです。</span><span class="sxs-lookup"><span data-stu-id="550db-224">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="550db-225">アプリケーションがサポートされていない構成を使用しようとした場合に警告するアナライザーが用意されています。</span><span class="sxs-lookup"><span data-stu-id="550db-225">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="550db-226">アプリでレガシ`IRouter`サポートが必要な場合`EnableEndpointRouting`は、で`Startup.ConfigureServices`次のいずれかの方法を使用しないようにします。</span><span class="sxs-lookup"><span data-stu-id="550db-226">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="550db-227">正常性チェックの移行</span><span class="sxs-lookup"><span data-stu-id="550db-227">Migrate health checks</span></span>

<span data-ttu-id="550db-228">正常性チェックは、エンドポイントルーティングを使用した*ルーターウェア*として使用できます。</span><span class="sxs-lookup"><span data-stu-id="550db-228">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="550db-229">エンド`MapHealthChecks`ポイントルーティングで正常性チェックを使用するには、を追加します。</span><span class="sxs-lookup"><span data-stu-id="550db-229">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="550db-230">メソッド`MapHealthChecks`は、のような`UseHealthChecks`引数を受け取ります。</span><span class="sxs-lookup"><span data-stu-id="550db-230">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="550db-231">`MapHealthChecks` Over`UseHealthChecks`を使用する利点は、承認を適用し、照合ポリシーをより詳細に制御できるようにすることです。</span><span class="sxs-lookup"><span data-stu-id="550db-231">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="550db-232">次の例では`MapHealthChecks` 、正常性チェックの`/healthz`エンドポイントに対してが呼び出されます。</span><span class="sxs-lookup"><span data-stu-id="550db-232">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="550db-233">HostBuilder が WebHostBuilder を置き換える</span><span class="sxs-lookup"><span data-stu-id="550db-233">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="550db-234">ASP.NET Core 3.0 テンプレートでは、[汎用ホスト](xref:fundamentals/host/generic-host)を使用します。</span><span class="sxs-lookup"><span data-stu-id="550db-234">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="550db-235">以前のバージョンの[Web ホスト](xref:fundamentals/host/web-host)が使用されていました。</span><span class="sxs-lookup"><span data-stu-id="550db-235">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="550db-236">次のコードは、ASP.NET Core 3.0 テンプレートに`Program`よって生成されたクラスを示しています。</span><span class="sxs-lookup"><span data-stu-id="550db-236">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="550db-237">次のコードは、ASP.NET Core 2.2 テンプレートで生成`Program`されたクラスを示しています。</span><span class="sxs-lookup"><span data-stu-id="550db-237">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="550db-238"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder>は3.0 に残り、は前のコード`webBuilder`サンプルで見たの型です。</span><span class="sxs-lookup"><span data-stu-id="550db-238"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="550db-239"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder>今後のリリースで非推奨となり、に`HostBuilder`置き換えられます。</span><span class="sxs-lookup"><span data-stu-id="550db-239"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="550db-240">から`WebHostBuilder` へ`HostBuilder`の最も重要な変更は、[依存関係の挿入 (DI)](xref:fundamentals/dependency-injection)です。</span><span class="sxs-lookup"><span data-stu-id="550db-240">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="550db-241">を使用`HostBuilder`する場合、 <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment>と`Startup`の<xref:Microsoft.Extensions.Configuration.IConfiguration>コンストラクターのみを挿入できます。</span><span class="sxs-lookup"><span data-stu-id="550db-241">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="550db-242">`HostBuilder` DI 制約:</span><span class="sxs-lookup"><span data-stu-id="550db-242">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="550db-243">DI コンテナーを1回だけビルドできるようにします。</span><span class="sxs-lookup"><span data-stu-id="550db-243">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="550db-244">シングルトンの複数のインスタンスを解決するなど、結果として得られるオブジェクトの有効期間の問題を回避します。</span><span class="sxs-lookup"><span data-stu-id="550db-244">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="550db-245">SignalR コードの更新</span><span class="sxs-lookup"><span data-stu-id="550db-245">Update SignalR code</span></span>

<span data-ttu-id="550db-246">`System.Text.Json`は、クライアントとサーバーの両方で使用される既定のハブプロトコルになりました。</span><span class="sxs-lookup"><span data-stu-id="550db-246">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="550db-247">で`Startup.ConfigureServices`、を`AddJsonProtocol`呼び出して、シリアライザーオプションを設定します。</span><span class="sxs-lookup"><span data-stu-id="550db-247">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="550db-248">**Server**</span><span class="sxs-lookup"><span data-stu-id="550db-248">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="550db-249">**クライアント:**</span><span class="sxs-lookup"><span data-stu-id="550db-249">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="550db-250">Newtonsoft. Json に切り替える</span><span class="sxs-lookup"><span data-stu-id="550db-250">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="550db-251">で`Newtonsoft.Json` `Newtonsoft.Json`サポートされていないの機能を使用している場合は、次のように切り替えることができます。 `System.Text.Json`</span><span class="sxs-lookup"><span data-stu-id="550db-251">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="550db-252">[AspNetCore. SignalR の json](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson)パッケージをインストールしてください。</span><span class="sxs-lookup"><span data-stu-id="550db-252">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="550db-253">クライアントで、 `AddNewtonsoftJsonProtocol`メソッド呼び出しを`HubConnectionBuilder`インスタンスにチェーンします。</span><span class="sxs-lookup"><span data-stu-id="550db-253">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="550db-254">サーバーで、 `AddNewtonsoftJsonProtocol`メソッド呼び出し`AddSignalR`をの`Startup.ConfigureServices`メソッド呼び出しにチェーンします。</span><span class="sxs-lookup"><span data-stu-id="550db-254">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="550db-255">ランタイムコンパイルをオプトインする</span><span class="sxs-lookup"><span data-stu-id="550db-255">Opt in to runtime compilation</span></span>

<span data-ttu-id="550db-256">3\.0 では、ランタイムコンパイルはオプトインシナリオです。</span><span class="sxs-lookup"><span data-stu-id="550db-256">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="550db-257">ランタイムコンパイルを有効にする https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation には、「」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="550db-257">To enable runtime compilation, see https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span></span>
